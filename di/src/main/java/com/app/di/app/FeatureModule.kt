package com.app.di.app

import dagger.Module

/**
 * This class should be responsible for providing the dependencies for app.
 * Make sure to provide dependencies here for every module's classes.
 * Include the module in app.
 */
@Module
class FeatureModule {
    //Provide dependencies here
}
